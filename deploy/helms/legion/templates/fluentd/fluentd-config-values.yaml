kind: ConfigMap
apiVersion: v1
metadata:
  name: "{{ .Release.Name }}-fluentd-config-values"
data:
  values.yaml: |
    common:
      bucket: "{{ .Values.feedback.s3.bucket }}"
      region: "{{ .Values.feedback.s3.region }}"

    default:
      path: "{{ .Values.feedback.s3.path }}"
      time_slice_format: "{{ .Values.feedback.s3.time_slice_format }}"
      time_slice_wait: "{{ .Values.feedback.s3.time_slice_wait }}"
      s3_object_key_format: "%{path}%{time_slice}_%{index}.%{file_extension}"
      store_as: "{{ .Values.feedback.s3.store_as }}"

      timekey: "{{ .Values.feedback.s3.buffering.timekey }}"
      timekey_wait: "{{ .Values.feedback.s3.buffering.timekey_wait }}"
      buffer_path: "{{ .Values.feedback.s3.buffering.path }}/default"

      format_type: "{{ .Values.feedback.s3.format }}"

    specific:
      tag1:
        path: "{{ .Values.feedback.s3.path }}"
        time_slice_format: "{{ .Values.feedback.s3.time_slice_format }}"
        time_slice_wait: "{{ .Values.feedback.s3.time_slice_wait }}"
        s3_object_key_format: "%{path}%{time_slice}_%{index}.%{file_extension}"
        store_as: "{{ .Values.feedback.s3.store_as }}"

        timekey: "{{ .Values.feedback.s3.buffering.timekey }}"
        timekey_wait: "{{ .Values.feedback.s3.buffering.timekey_wait }}"
        buffer_path: "{{ .Values.feedback.s3.buffering.path }}/tag1"

        format_type: "{{ .Values.feedback.s3.format }}"
